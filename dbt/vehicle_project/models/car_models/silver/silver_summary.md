{% docs silver_summary_table %}
Cleaned summary specifications for a vehicle model (range, charging times, performance).
This table keeps only the most recent scrape per model and converts raw strings
into structured numeric fields suitable for analytics.
{% enddocs %}