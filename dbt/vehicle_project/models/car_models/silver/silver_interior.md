{% docs silver_interior_table %}
Cleaned boolean interior feature flags extracted from the vehicle’s specification page.
Only the most recent scrape per model is kept. These fields represent whether
individual interior features are present (`true`) or absent (`false`).
{% enddocs %}